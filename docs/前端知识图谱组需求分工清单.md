# 前端知识图谱组任务清单

## 1. 使用方式

这份文档只罗列前端和知识图谱组需要完成的任务，不写人员安排。组内可以根据任务编号自行分配。

每条任务按以下口径记录：

```text
任务编号
当前状态
任务内容
输入
输出
验收标准
风险或备注
```

状态建议统一使用：

```text
已完成
待实机验证
待联调
进行中
未开始
阻塞
```

## 2. 当前总体状态

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| mock 数据闭环 | 已完成 | 已有新能源汽车 mock，可生成非空节点、公司边、画像边和证据，并已完成 Neo4j persisted 验证。 |
| KG 服务接口 | 已完成 | 已有 `/health`、`/api/graph/build`、`/api/graph/ingest`、`/api/graph/visualization` 等接口。 |
| graph_payload 校验 | 已完成 | 已新增 schema 校验，入库前会拦截结构错误。 |
| Neo4j 主存储写入 | 已完成 | Docker Neo4j 实机验证已通过，`/api/graph/ingest` 可返回 `persisted`。 |
| Neo4j 查询转前端图谱 | 已完成 | 已验证 `visualization_payload` 返回节点、关系、公司边和画像边。 |
| 前端图谱工作台 | 已完成 | `npm run build` 已通过，`http://localhost:3001` 已启动并返回 200，主流程调用 `/api/graph/ingest`，已吸收 PR 中的图谱布局稳定性优化。 |
| 前端交互验收 | 待人工点击验证 | 前端 build 和首页 200 已验证；多公司展开、节点位置锁定、边详情仍需浏览器手动确认。 |
| 后端 Agent 组对接 | 待联调 | 接口协议已写好，待后端提供真实 `insight_payload`。 |
| 第一周演示路径 | 进行中 | Docker Neo4j、KG 服务和前端已本机启动，mock 入库已通过；仍需补充浏览器点击截图和 Neo4j Browser 查询截图。 |

## 3. T1 mock 数据闭环

当前状态：已完成。

任务内容：

```text
用新能源汽车 mock 数据跑通 insight_payload -> graph_payload -> Neo4j -> 前端图谱展示。
```

输入：

```text
samples/new-energy-insight-payload.json
frontend/public/samples/new-energy-insight-payload.json
```

输出：

```text
graph_payload
visualization_payload
前端图谱展示
```

验收标准：

- 前端可以点击“加载 mock”。
- `/api/graph/build` 可生成 `graph_payload`。
- `/api/graph/ingest` 在 Neo4j 正常时返回 `persistence_meta.status=persisted`。
- `graph_nodes.length > 0`
- `company_edges.length > 0`
- `profile_edges.length > 0`
- `evidence_chunks.length > 0`

风险或备注：

- 最新实机验证 trace_id：`kg_20260702_1d7ccda485`。
- 如果 Neo4j 没启动，主流程会返回 `failed`，这是预期行为。

## 4. T2 KG 服务接口

当前状态：已完成。

任务内容：

```text
提供前端和后端 Agent 组都能调用的知识图谱 REST 接口。
```

接口清单：

```text
GET  /health
POST /api/graph/build
POST /api/graph/ingest
GET  /api/graph/visualization
POST /api/graph/persist
POST /api/graph/build-and-persist
```

主接口：

```text
POST /api/graph/ingest
```

输出：

```text
graph_payload
visualization_payload
persistence_meta
```

验收标准：

- `/health` 返回 `status=ok`。
- `/api/graph/build` 返回 `graph_payload` 和 `validation_meta`。
- `/api/graph/ingest` 完成生成、校验、入库和查询转换。
- Neo4j 缺配置时返回 `failed`，包含 `reason`、`error_type`、`trace_id`。
- 显式设置 `KG_ALLOW_GRAPH_PAYLOAD_FALLBACK=true` 时才允许 `skipped`。

风险或备注：

- `/api/graph/build` 只用于 debug，不是最终主链路。
- 本机启动验证：`http://127.0.0.1:8008/health` 已返回 `status=ok`。

## 5. T3 graph_payload schema 校验

当前状态：已完成。

任务内容：

```text
在 graph_payload 写入 Neo4j 前完成结构校验，避免脏数据进入主存储。
```

输入：

```text
graph_payload
```

输出：

```text
validation_meta
或 persistence_meta.status=failed
```

验收标准：

- `graph_nodes` 必须是数组。
- `relation_candidates` 必须是数组。
- `graph_edges` 必须是数组。
- `evidence_chunks` 必须是数组。
- `graph_edges` 只允许 `verified` 和 `strong_candidate`。
- `graph_edges[].head` 和 `graph_edges[].tail` 必须能找到对应节点。
- 校验失败时不调用 Neo4j 写入。

风险或备注：

- 后端真实数据接入时，如果字段结构变化，需要同步更新校验规则。

## 6. T4 Neo4j 主存储写入

当前状态：已完成。

任务内容：

```text
把 graph_payload 作为中间格式，校验通过后强制写入 Neo4j 主存储。
```

输入：

```text
graph_payload
trace_id
project_id
document_id
version
```

输出：

```text
Neo4j 节点
Neo4j 明确关系类型
RelationFact
EvidenceChunk
Source
persistence_meta
```

验收标准：

- 写入使用事务。
- 节点和关系使用 `MERGE`。
- 已创建唯一约束和查询索引。
- 成功返回 `status=persisted`。
- 成功返回 `node_count`、`relation_count`、`trace_id`、`created_at`。
- 失败返回 `status=failed`、`reason`、`error_type`、`trace_id`。
- 不默认返回 `skipped`。

验证 Cypher：

```cypher
MATCH (a)-[r]->(b)
WHERE r.rel_id IS NOT NULL
RETURN a.name AS head, type(r) AS relation, b.name AS tail, r.confidence AS confidence
LIMIT 25;
```

风险或备注：

- 最新实机验证结果：`status=persisted`，`node_count=43`，`relation_count=22`，`evidence_count=27`。
- 最新 trace_id：`kg_20260702_1d7ccda485`。
- 后续如果同一实体跨项目复用，需要继续完善 `project_id` 维度的隔离策略。

## 7. T5 Neo4j 查询转前端图谱

当前状态：已完成。

任务内容：

```text
前端展示优先使用 Neo4j 查询结果，而不是直接依赖 graph_payload。
```

输入：

```text
trace_id
project_id
```

输出：

```text
visualization_payload.graph_nodes
visualization_payload.graph_edges
visualization_payload.company_edges
visualization_payload.profile_edges
visualization_payload.evidence_chunks
```

验收标准：

- `/api/graph/visualization?trace_id=<trace_id>` 能返回图谱展示数据。
- `company_edges` 可用于公司关系图。
- `profile_edges` 可用于公司画像展开。
- `evidence_chunks` 可用于证据追溯。
- 返回结构能被前端 `IndustryGraphView` 直接消费。

风险或备注：

- 最新实机验证结果：`graph_nodes=60`，`graph_edges=43`，`company_edges=2`，`profile_edges=32`，`evidence_chunks=49`。
- 如果 Neo4j 中没有对应 `trace_id`，前端应显示空状态。

## 8. T6 前端图谱结果区

当前状态：构建和页面访问已完成，待浏览器点击验收。

任务内容：

```text
提供独立前端图谱工作台，用于 mock 验证、后端 JSON 粘贴和 Neo4j 主链路展示。
```

功能清单：

- 加载 mock。
- 粘贴后端 JSON。
- debug 生成 `graph_payload`。
- 主流程调用 `/api/graph/ingest`。
- 显示节点数、公司边、画像边、入库状态。
- 展示产业链摘要。
- 展示公司关系图。
- 点击公司节点展开画像关系。
- 支持同时展开多个公司节点。
- 首次布局后锁定公司节点位置，展开画像时减少公司节点跳动。
- 画像节点按业务线、产品线、核心技术分组摆放在公司节点周围。
- 展开公司节点时进行视觉高亮。
- 点击关系边展示证据、来源、置信度和状态。
- Neo4j 失败时显示 `failed`、原因和 `trace_id`。

验收标准：

- `npm run build` 通过。
- 页面能打开 `http://localhost:3001`，HTTP 返回 200。
- 点击“生成并写入 Neo4j”后，图谱不空白。
- 点击节点和边有响应。
- 点击多个公司节点后，可以同时展开多个画像区域。
- 展开画像关系时，公司节点位置不发生明显跳动。
- 画像边和公司边视觉上可区分。
- 空状态和失败态信息清楚。

风险或备注：

- 当前前端只覆盖图谱工作台，不是最终完整 Web 应用页面。
- 当前布局优化已经完成代码合入、`npm run build` 验证和首页 HTTP 访问验证。
- 本机前端已启动：`http://localhost:3001` 返回 200。
- 仍需要真实浏览器手动点击确认多公司展开、边详情和节点稳定性。

## 9. T7 后端 Agent 组对接

当前状态：文档已完成，待联调。

任务内容：

```text
明确后端 Agent 组输出 insight_payload 的字段要求，并用真实输出跑通 /api/graph/ingest。
```

后端必须提供：

```text
task_profile
chain_skeleton
segment_companies
company_cards
source_index
```

重点字段：

```text
company_cards[].relationship_clues
company_cards[].field_evidence
company_cards[].key_sources
company_cards[].evidence_excerpts
source_index[].source_grade
```

验收标准：

- 后端真实 `insight_payload` 可以直接传给 `/api/graph/ingest`。
- 结构化 `relationship_clues` 能生成非空 `company_edges`。
- 字段级 `field_evidence` 能生成有证据的 `profile_edges`。
- 来源链接和来源等级能显示在前端边详情里。

风险或备注：

- 如果后端字段缺失，图谱可以生成但边或证据可能不足。
- 如果后端输出关系线索不结构化，公司关系边可能为空。

## 10. T8 第一周演示路径

当前状态：进行中。

任务内容：

```text
形成一个可复现的第一周演示流程，用 mock 数据展示知识图谱端到端闭环。
```

演示步骤：

```text
1. 启动 Neo4j。
2. 配置 NEO4J_URI、NEO4J_USER、NEO4J_PASSWORD。
3. 启动 KG 服务。
4. 启动前端。
5. 打开 http://localhost:3001。
6. 点击“加载 mock”。
7. 点击“生成并写入 Neo4j”。
8. 展示 persisted、trace_id、节点数、公司边、画像边、证据片段数量。
9. 点击多个公司节点，展示多个画像关系区域。
10. 观察公司节点位置是否保持稳定。
11. 点击关系边，展示证据文本和来源链接。
12. 在 Neo4j Browser 按 trace_id 查询节点、关系和证据。
13. 说明后端 Agent 组接入时只需要替换 insight_payload。
```

验收标准：

- 演示全程不依赖真实搜索。
- 演示全程可以解释 `insight_payload -> graph_payload -> Neo4j -> 前端图谱`。
- 能展示成功状态和失败状态。
- 能说明 `graph_payload` 是中间格式，Neo4j 是主存储。

风险或备注：

- 当前本机端口已验证：前端 `3001`、KG 服务 `8008`、Neo4j Browser `7474`、Neo4j Bolt `7687` 均可用。
- 演示当天必须提前确认 Docker、Neo4j、KG 服务、前端端口没有冲突。

## 11. T9 验证记录

当前状态：进行中。

任务内容：

```text
完成一次真实本地验证后，把验证日期、trace_id、Neo4j 查询结果和前端截图记录下来。
```

输入：

```text
mock insight_payload
Neo4j Browser 查询结果
前端页面截图
```

输出：

```text
第一周验证记录
```

验收标准：

- 记录 `persistence_meta.status`。
- 记录 `trace_id`。
- 记录节点数量。
- 记录关系数量。
- 记录证据数量。
- 记录前端图谱是否可点击。
- 记录多公司展开是否正常。
- 记录展开画像时公司节点是否明显跳动。
- 记录 Neo4j Browser 是否能按 trace_id 查到结果。

风险或备注：

- 没有验证记录时，代码完成但汇报说服力不足。

最新验证记录摘要：

```text
验证时间：2026-07-02 14:58:20 +08:00
trace_id：kg_20260702_1d7ccda485
persistence_meta.status：persisted
node_count：43
relation_count：22
evidence_count：27
graph_nodes：60
graph_edges：43
company_edges：2
profile_edges：32
evidence_chunks：49
KG 服务：http://127.0.0.1:8008/health 已返回 ok
前端页面：http://localhost:3001 已返回 200
```

## 12. 第一周最小可交付清单

第一周最低可交付必须满足：

- mock 可加载。
- `/api/graph/ingest` 可调用。
- Neo4j 可返回 `persisted`。
- 前端可展示公司关系图。
- 点击公司节点能展开画像关系。
- 支持多公司同时展开画像关系。
- 展开画像时公司节点位置基本稳定。
- 点击关系边能看到证据和来源。
- 后端 Agent 组知道需要提供哪些字段。
- 有一份验证记录，包含 `trace_id` 和 Neo4j Browser 查询结果。

## 13. 不放入第一周的任务

以下任务进入第二周或第三周：

- 图谱节点筛选。
- 边详情弹窗优化。
- 按企业查询上下游。
- 按技术查询相关公司。
- Neo4j 多项目数据隔离增强。
- 图谱质量评分和冲突标记。
- GraphRAG 问答。
- CEO 决策建议模块。
